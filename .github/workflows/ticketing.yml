name: Run Ticketing Report Dayly/manual
on:
  schedule:
    - cron: "0 17 * * *"
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          pip install pygsheets psycopg2-binary

      - name: Create credentials files
        run: |
          cat TKT/client_secret.json > client_secret.json
          cat TKT/sheets.googleapis.com-python.json > sheets.googleapis.com-python.json

      - name: Run script
        run: python ticketingResumen.py
